<p id="notice"><%= notice %></p>

<h1>Breastfeedings</h1>

<table>
  <thead>
    <tr>
      <th>id</th>
      <th>Volume</th>
      <th>Feeding time in seconds</th>
      <th>Baby</th>
      <th>Breasfeeding time</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @breastfeedings.each do |breastfeeding| %>
      <tr>
        <td><%= breastfeeding.id %></td>
        <td><%= breastfeeding.volume %></td>
        <td><%= breastfeeding.feeding_time %></td>
        <td><%= breastfeeding.baby.name %></td>
        <td><%= breastfeeding.created_at %></td>
        <td><%= link_to 'Show', breastfeeding %></td>
        <td><%= link_to 'Edit', edit_breastfeeding_path(breastfeeding) %></td>
        <td><%= link_to 'Destroy', breastfeeding, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Breastfeeding', new_breastfeeding_path %>
